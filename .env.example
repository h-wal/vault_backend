# PostgreSQL connection string for vault_backend
# Update with your actual credentials if different
DATABASE_URL=postgres://vault:vaultpassword@localhost/vault_backend

# Enable offline mode for cargo sqlx to use cached query metadata
# Set to 'true' for CI/CD pipelines where database is not available
SQLX_OFFLINE=false
